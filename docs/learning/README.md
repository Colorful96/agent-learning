# Learning Notes

This directory contains the chronological learning notes created while building the project.

These notes explain the evolution from a command-line summarizer to the current FastAPI, RAG and LangGraph application. They are useful for understanding design decisions, but the current runnable entrypoints are documented in the repository root `README.md`.
